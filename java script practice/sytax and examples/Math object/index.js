
// Math built-in object that provides a collection of properties and methods

let x = 3;
let y = 2;
let z = 1;
//z = Math.round(x);
//z=Math.floor(x); this rounds down the number
//z=Math.ceil(x); this rounds up the number
//z=Math. trunc(x); this removes the  decimal numbers
//z=Math.log(x);
//z=Math.pow(x, y);
//z=Math.sqrt(x);
//z=Math.sin(x);
//z=Math.cos(x);
//z=Math.tan(x);
//z=Math.abs(x);
//z=Math.sign(x);
let max= Math.max(x, y, z);
let min = Math.min(x, y, z);
console.log(min); //log is the print in java script